export default function PostDetails({item}) {
    return (
        <div className={'fuck'}>
            <h2>{item.id}</h2>
            <h3>{item.title}</h3>
            <p>{item.body}</p>
        </div>
    )
}